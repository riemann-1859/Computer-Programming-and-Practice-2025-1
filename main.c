/* include 지시어 */ 

/* 문법 
 * # include " 파일이름 " 엔터 
 *
 * 의미
 * 파일이름에 해당하는 파일을 현재 폴더에서 찾고, 없으면 지정된 폴더에서, 또 없으면 표준 라이브러리 폴더에서 찾습니다.
 * 파일의 내용을 현재 줄에 모두 포함시킵니다. 
 */

/* To Do: value 함수에 대한 선언이 있는 value.h 파일을 포함시켜보세요 */

int main() {
	return value(1); /* 1을 반환한다. */
}
